#include <stdlib.h>

char *are_you_playing_banjo(const char *name) {
    char* playsbanjo = " plays banjo";
    char* doesnotplays = " does not play banjo";
    if (name[0] =='R' || name[0] == 'r') {
        
    }
    
    return calloc(1, 1); // memory will be freed
}
